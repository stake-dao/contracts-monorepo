Accountant__setProtocolFeePercent
├── given protocol fee is 0
│   └── it stores the new value
├── given protocol fee is in the range
│   └── it stores the new value
├── given protocol fee is out the range
│   └── it reverts
├── given protocol fee is higher than max
│   └── it reverts
├── given protocol fee is equal to the old value
│   └── it stores the new value
├── it never updates the harvest fee
├── it updates the fees slot
├── when it emits the event
│   └── it emits the new protocol fee
│   └── it emits the old protocol fee
├── given caller is not owner
│   └── it reverts
└── given caller is owner
    └── it never reverts
