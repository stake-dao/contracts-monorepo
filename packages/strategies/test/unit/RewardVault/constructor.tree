RewardVault__constructor
├── it revert if accountant is zero address
├── it revert if protocolController is zero address
├── it set protocolId
├── it set accountant
├── it set protocolController
├── it mint 0 shares to itself
└── it set ERC20 metadata

