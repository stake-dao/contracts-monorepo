pragma solidity 0.8.28;

import {RewardVaultBaseTest} from "test/RewardVaultBaseTest.sol";

contract RewardVault__rewardPerToken is RewardVaultBaseTest {
    function test_ReturnsTheRewardPerTokenStoredWhenTotalSupplyIs0() external {
        // it returns the reward per token stored when total supply is 0
    }

    function test_CalculatesTheRewardPerTokenBasedOnItsData() external {
        // it calculates the reward per token based on its data
    }
}
